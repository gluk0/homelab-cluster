---
apiVersion: v1
kind: Service
metadata:
  name: matrix-synapse
  namespace: matrix
  labels:
    app: matrix-synapse
spec:
  type: ClusterIP
  selector:
    app: matrix-synapse
  ports:
  - port: 8008
    targetPort: 8008
    protocol: TCP
    name: http
