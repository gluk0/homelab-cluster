---
apiVersion: v1
kind: Service
metadata:
  name: website
  namespace: default
  labels:
    app: website
spec:
  type: ClusterIP
  selector:
    app: website
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
