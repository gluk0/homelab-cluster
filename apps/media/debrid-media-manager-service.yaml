---
apiVersion: v1
kind: Service
metadata:
  name: debrid-media-manager
  namespace: media
  labels:
    app: debrid-media-manager
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: debrid-media-manager
